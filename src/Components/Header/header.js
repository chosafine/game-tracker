import React from "react";

function Header() {
    return (
      <div>
        <h1>Game Tracker</h1>
        <h4>To get started type in a game name into the search bar:</h4>
      </div>
    );
}

export default Header;